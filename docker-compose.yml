services:
  mcp-lazy-proxy:
    image: sarthakpranesh/mcp-lazy-proxy
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./mcp.json:/app/mcp.json:ro
    restart: unless-stopped
