# Shared bearer token clients must use to reach the proxy's /mcp endpoint.
# Required in HTTP mode; the proxy refuses to start without it.
MCP_AUTH_TOKEN=change-me-to-a-long-random-string

# Backend secrets referenced in mcp.json via ${GITHUB_TOKEN} etc.
GITHUB_TOKEN=
